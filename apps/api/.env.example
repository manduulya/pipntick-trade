DATABASE_URL=postgresql://user:password@localhost:5432/pipntick

# Leave these unset for local dev and every request is attributed to
# DEV_USER_ID instead (see src/lib/auth.ts). Set both to enable real auth.
CLERK_SECRET_KEY=sk_test_your_key_here
CLERK_PUBLISHABLE_KEY=pk_test_your_key_here
# DEV_USER_ID=user_dev_001

CORS_ORIGIN=http://localhost:3000
PORT=3001
